services:
  db:
    image: postgres:16-alpine
    container_name: carbon_advisor_db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: carbon_db
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
