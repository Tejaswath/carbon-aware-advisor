# Legacy Gradio Prototype

`gradio_app.py` is the pre-pivot prototype UI and is kept only for historical reference.

Current production/demo path:
- Backend: `backend/app/main.py` (FastAPI + LangGraph)
- Frontend: `frontend/app/page.tsx` (Next.js routing-first console)

The legacy file is intentionally isolated to prevent confusion during code review.
